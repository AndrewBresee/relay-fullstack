schema {
  query: Query
}

type GoogleUser implements Node {
  id: ID!
  user: Int
  givenName: String
  familyName: String
}

interface Node {
  id: ID!
}

type Query {
  node(id: ID!): Node
  viewer: GoogleUser
}
